#include <iostream>
#include <vector>
using namespace std;

//점점 해시 함수를 묻는 형태가 늘어나고 있음
// 해시 함수를 터득해야함

// + 우선순위 큐도 필요
int main()
{
	int n;
	cin >> n;
	vector<int> num;
	int tmp;
	for (int i = 0; i < n; i++) {
		cin >> tmp;
		if (n != 0) {
			if (num.size() == 0) {
				num.push_back(tmp);
			}
			else {
				
			}
		}
		
	}
}
